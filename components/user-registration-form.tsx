import { View } from 'react-native';

/**
 * Formulário de cadastro de usuário — implementação intencionalmente ausente
 * neste passo (TDD: testes definem o contrato antes da UI).
 */
export default function UserRegistrationForm() {
  return <View testID="user-registration-form-root" />;
}
